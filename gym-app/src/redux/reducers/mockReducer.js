import { diet } from '../../share/mockData';

const mockReducer = (state = diet, action) => {
    switch(action.type) {

        default:
            return state;
    }
}

export default mockReducer;
